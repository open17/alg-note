#include <bits/stdc++.h>
using namespace std;

#define int long long
#define endl '\n'
#define ios ios::sync_with_stdio(false);cin.tie(0);cout.tie(0);
#define read() ({ int x = 0, f = 1; char ch = getchar(); while (ch < '0' || ch > '9') { if (ch == '-') f = -1; ch = getchar(); } while (ch >= '0' && ch <= '9') { x = x * 10 + ch - '0'; ch = getchar(); } x * f; })
  
const int N = 2e5 + 10;

signed main()
{
    // ios;
    int a = read();
    cout<<a;
    return 0;
}